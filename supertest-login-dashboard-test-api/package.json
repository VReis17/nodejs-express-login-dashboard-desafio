{
  "name": "supertest-login-dashboard-test-api",
  "version": "1.0.0",
  "description": "Testes automatizados da API de login e dashboard usando SuperTest e Mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require ./hooks/globalHooks.js tests/**/*.test.js --timeout 10000",
    "test:watch": "mocha --require ./hooks/globalHooks.js tests/**/*.test.js --watch --timeout 10000",
    "test:coverage": "nyc mocha --require ./hooks/globalHooks.js tests/**/*.test.js --timeout 10000",
    "test:report": "mocha --require ./hooks/globalHooks.js tests/**/*.test.js --reporter mochawesome --reporter-options reportDir=./reports,reportFilename=test-report,reportTitle='Relatório de Testes API' --timeout 10000"
  },
  "keywords": [
    "test",
    "api",
    "supertest",
    "mocha",
    "login"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "chai": "^4.3.7",
    "dotenv": "^17.2.1",
    "faker": "^6.6.6",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0",
    "nyc": "^15.1.0",
    "supertest": "^6.3.3"
  }
}
