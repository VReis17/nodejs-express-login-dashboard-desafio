{
  "name": "supertest-login-dashboard-test-api",
  "version": "1.0.0",
  "description": "Testes automatizados da API de login e dashboard usando SuperTest e Mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:watch": "mocha tests/**/*.test.js --watch --timeout 10000",
    "test:coverage": "nyc mocha tests/**/*.test.js --timeout 10000"
  },
  "keywords": ["test", "api", "supertest", "mocha", "login"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^10.2.0",
    "supertest": "^6.3.3",
    "chai": "^4.3.7",
    "nyc": "^15.1.0"
  }
} 